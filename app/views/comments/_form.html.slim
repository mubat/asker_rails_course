= form_with model: comment, local: false, class: 'new-comment' do |f|
  = f.hidden_field :commentable_id, value: commentable.class
  = f.hidden_field :commentable_type, value: commentable.id
  = f.label :body, 'Comment'
  = f.text_area :body
  = f.submit 'Add comment'

